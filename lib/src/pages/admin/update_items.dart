import 'package:flutter/material.dart';

class UpdateItemsPage extends StatelessWidget {
  const UpdateItemsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Update Items Page"),
    );
  }
}